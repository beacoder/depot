<!-- [128] update: use t helper function to translate title -->
<h2><%= t('.title') %></h2>
<table>

  <!-- [45] update: define the partial template for cart,
       implicit current object is cart which matches the template name -->
  <%= render(cart.line_items) %>

  <tr class="total_line">
    <td colspan="2">Total</td>
    <td class="total_cell"><%= number_to_currency(cart.total_price) %></td>
  </tr>

</table>

<!-- [70] update: add button to create order for current cart -->
<!-- <%= button_to "Checkout", new_order_path, method: :get %> -->

<!-- <%= button_to 'Empty cart', cart, method: :delete, data: { confirm: 'Are you sure?' } %> -->

<!-- [129] update: use t helper function to translate cart -->
<%= button_to t('.checkout'), new_order_path, method: :get %>
<%= button_to t('.empty'), cart, method: :delete, data: { confirm: 'Are you sure?' } %>
